.navbar.navbar-default.navbar-fixed-top(role='navigation')
  .container
    .navbar-header
      button.navbar-toggle(type='button',data-toggle='collapse', data-target='.navbar-collapse')
        span.sr-only Toggle navigation
        span.icon-bar
        span.icon-bar
        span.icon-bar
      a.navbar-brand(href='/')=title
    .collapse.navbar-collapse
      ul.nav.navbar-nav
        li#nav-home
          a(href='/') Home
        //- to wait upgrade ins api
        //- li#nav-photo
        //-  a(href='/photo') Photo
        li
          a(href='http://wp.joak.org') Blog
        li
          a(href='http://wiki.joak.org') Wiki
        li 
          a(href='/tools') Tools
        li#nav-about
          a(href='/about') About
      a.btn.btn-info.navbar-btn.navbar-right.disabled(href=login ? '/logout' : '/login')=login ? 'Sign out' : 'Sign in'
      if login
        p.navbar-text.navbar-right Signed in as 
          a.navbar-link(href='#',style='margin-left:5px')=user.account
script(type='text/javascript').
  var path = window.location.pathname;
  var page = '/' + path.split('/')[1];
  page = page.toLowerCase();
  $('a[href="' + page + '"]').parent().addClass('active');
